<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= settings.appName %> - Login</title>
  <link rel="stylesheet" href="/static/style.css">
  <style>
    body::before{
      background-image: linear-gradient(rgba(12,8,8,.68), rgba(12,8,8,.84)), url('<%= settings.backgroundImageUrl || "/static/default-bg.svg" %>');
      filter: blur(<%= settings.blurStrength || 14 %>px);
      transform: scale(1.06);
    }
  </style>
</head>
<body class="login-page">
  <div class="overlay"></div>
  <div class="login-card glass">
    <div class="login-inner">
      <div class="brand-wrap">
        <div class="top-badge">IRAQ STORE ANNOUNCEMENTS</div>
        <img src="<%= logoPath() %>" class="brand-logo" alt="Iraq Store Logo">
        <h1 class="brand-title"><%= settings.appName %></h1>
        <p class="brand-desc"><%= settings.siteDescription %></p>
      </div>
      <div class="developer-box">
        <img src="<%= developer.avatar %>" class="developer-avatar" alt="Developer">
        <div class="developer-meta">
          <span class="developer-label">Developer</span>
          <span class="developer-name"><%= developer.displayName %></span>
          <span class="developer-user">@<%= developer.username %></span>
        </div>
      </div>
      <% if (error) { %><div class="alert error"><%= error %></div><% } %>
      <form method="POST" action="/login">
        <div class="field"><label>الإيميل</label><input type="email" name="email" required></div>
        <div class="field"><label>كلمة المرور</label><input type="password" name="password" required></div>
        <button class="primary-btn full-btn" type="submit">دخول</button>
      </form>
    </div>
  </div>
</body>
</html>
